version: '3'
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: npm run start:dev
    environment:
      NODE_ENV: development
    ports:
      - '8000:8000'
    volumes:
      - .:/usr/src/app
